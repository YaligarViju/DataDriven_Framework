package snippet;

public class Snippet {
	public static void main(String[] args) {
		if (condition.contains("Page List")) {
					restUtil.setEndpoint(string);
				} else if (condition.contains("Create User")) {
					restUtil.setEndpoint(string);
				} else if (condition.contains("Update User") || condition.contains("Get User")
						|| condition.contains("Delete User") || condition.contains("Partial Update")) {
		//			restUtil.setEndpoint(string+pojo.getUserId);
					restUtil.setEndpoint(string);
				}
	}
}

